# RPMStream runtime dependencies
#
# kurigram  — maintained Pyrogram fork (MTProto, efficient large file reads)
# aiohttp   — async HTTP client used to stream multipart uploads to RPMShare
# TgCrypto  — C speedup for MTProto encryption (big win on small VPS)

kurigram>=2.2.25,<3.0
aiohttp>=3.9,<4.0
python-dotenv>=1.0,<2.0
TgCrypto>=1.2.5,<2.0
